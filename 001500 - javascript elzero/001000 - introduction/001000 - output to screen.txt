Output to screen
    console
        it is not in javascript > it is one of many web apis
        in javascript is an object that allows you to log messages
            when js works in a browser > console logs messages to console window in developer tools

            when js works in node js it will log the messages to the terminal

        console.log("message")
        console.table({})

    document.write("message")

    window.alert
        rarely used