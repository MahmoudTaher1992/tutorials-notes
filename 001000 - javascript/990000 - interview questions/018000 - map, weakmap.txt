Map
    An object that holds key-value pairs

    key and value can be primitive/non primitive types

WeakMap
    An object that holds key-value pairs

    keys must be objects (non primitive data type)

    It is weak > if no other reference exists to the ket object > garbage collector can destroy it

