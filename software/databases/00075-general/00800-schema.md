# Schema

* schema is the structure the will hold and organize data
* CREATE
    * TABLE
    * COLUMN
    * INDEX
    * VIEW
        * virtual table
    * SEQUENCE/AUTO_INCREMENT

* ALTER
    * COLUMN
        * ADD
        * DROP
    * CONSTRAINT
        * ADD
        * DROP

* RENAME